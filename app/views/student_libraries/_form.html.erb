<%= form_for([@student_library.faculty, @student_library]) do |f| %>
  <% if @student_library.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_library.errors.count, "error") %> prohibited this student_library from being saved:</h2>

      <ul>
      <% @student_library.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
